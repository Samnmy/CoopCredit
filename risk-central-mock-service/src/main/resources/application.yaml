# application.yml para Risk Central Mock Service
server:
  port: 8081

spring:
  application:
    name: risk-central-mock-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.coopcredit.riskcentralmockservice: INFO